cat > .claude/commands/add.md << 'EOF'
Add a feature: $ARGUMENTS

I'll add this with minimal changes.

Examples:
- /add dark mode
- /add export button
- /add user preferences

I'll keep it simple and focused.
EOF
